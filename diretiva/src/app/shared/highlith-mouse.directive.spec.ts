import { HighlithMouseDirective } from './highlith-mouse.directive';

describe('HighlithMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlithMouseDirective();
    expect(directive).toBeTruthy();
  });
});
